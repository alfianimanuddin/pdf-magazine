# Database
# For docker-compose: Use this format with URL-encoded password if it contains special characters
# Special chars like @!#$%^&/:? must be URL-encoded (e.g., @ becomes %40, ! becomes %21)
DATABASE_URL="postgresql://postgres:your_secure_password_here@postgres:5432/magazine_platform"
DB_PASSWORD="your_secure_password_here"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Admin Credentials (First user)
ADMIN_EMAIL="admin@tadatodays.com"
ADMIN_PASSWORD="changeme123"

# File Storage
UPLOAD_DIR="./public/uploads"
MAX_FILE_SIZE=52428800 # 50MB in bytes

# App Settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"
